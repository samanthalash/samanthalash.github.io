# Legacy Static Site

This folder contains the previous HTML-first portfolio scaffold that existed before the React + Vite rebuild.

- It is preserved for reference only.
- It is not part of the live application entry path.
- Some absolute links inside these files still reflect the old root-level structure, so treat this directory as an archive rather than an active build target.
