# Archive Preservation

The user explicitly requires the previous portfolio to remain functional. Never
delete or rename anything in `src/archive/portfolio/`, `inspo/`, or
`archive/legacy-site/` as part of the new build. Copy required assets into the new
application and verify `/archive/` after routing or build changes.

