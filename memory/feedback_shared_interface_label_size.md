# Shared Interface Label Size

Use the landing page's top-right `About Me` and `+ Projects` navigation size as
the type-size authority for interface labels. The landing project number/title
and the About-page headings `About Me`, `Experience`, `Education`, `Skills`,
`Languages`, and `Contact` must use that same responsive size.
