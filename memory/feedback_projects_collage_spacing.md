# Projects Collage Gap Consistency

On the Projects selected-work page, image pairs that directly face one another
must share the same gap. Position adjacent tiles relationally from a single gap
value rather than tuning every coordinate independently. Preserve the supplied
images' intrinsic aspect ratios when correcting spacing.
