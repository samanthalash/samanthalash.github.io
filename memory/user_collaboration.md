# User Working Preferences

- Continue through implementation and verification without stopping at a proposal
  when the requested work is actionable.
- Organize repository knowledge so future sessions can recover intent quickly.
- When the user corrects behavior or asks for something to be remembered, create
  a focused memory file and index it in `memory/MEMORY.md`.
- Keep project tracking and documentation current after major work rather than
  relying on chat history.

